spring:
  application:
    name: xcplus-content-api
  cloud:
    nacos:
      discovery:
        server-addr: centos006:1005
        namespace: ns-xcplus-dev
        group: content
server:
  port: 1002
  servlet:
    context-path: /content #项目路径，构成url地址的一部分。
logging:
  config: classpath:log4j2-dev.xml

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.8</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.zkc</groupId>
    <artifactId>xcplus</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>xcplus</name>
    <packaging>pom</packaging>

    <modules>
        <module>xcplus-base</module>
        <module>xcplus-content</module>
    </modules>

    <properties>
        <java.version>1.8</java.version>
        <springboot.starter.version>2.7.8</springboot.starter.version>
        <springboot.starter.test.version>2.7.8</springboot.starter.test.version>
        <lombok.version>1.18.26</lombok.version>
        <mybatisplus-core.version>3.5.1</mybatisplus-core.version>
        <xcplus-base.version>0.0.1-SNAPSHOT</xcplus-base.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
            <version>${springboot.starter.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <version>${springboot.starter.test.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>


    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-core</artifactId>
                <version>${mybatisplus-core.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zkc</groupId>
                <artifactId>xcplus-base</artifactId>
                <version>${xcplus-base.version}</version>
            </dependency>
        </dependencies>

    </dependencyManagement>


    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
